common:
  image_size: 1024
  batch: 1
  num_workers: 2
  device: cuda
train:
  epochs: 1
  max_train_steps: 8
  max_val_images: 16
model:
  pretrained_model_name: facebook/mask2former-swin-tiny-coco-instance
  score_threshold: 0.2
  mask_threshold: 0.5
